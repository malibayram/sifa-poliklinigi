import 'package:flutter/material.dart';

class TibbiSekreter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tıbbi Sekreter Sayfası")),
    );
  }
}
